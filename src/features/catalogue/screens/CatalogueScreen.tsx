import PlaceholderScreen from "../../../shared/components/PlaceholderScreen";

export default function CatalogueScreen() {
    return (
        <PlaceholderScreen
            title="Catalogue"
            description="Manage products, categories, and catalogue structure across vendors."
            icon="BookOpen"
        />
    );
}
