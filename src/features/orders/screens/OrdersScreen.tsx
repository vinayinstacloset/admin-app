import PlaceholderScreen from "../../../shared/components/PlaceholderScreen";

export default function OrdersScreen() {
    return (
        <PlaceholderScreen
            title="Orders"
            description="Browse, filter, and manage every order placed across all vendors."
            icon="ClipboardList"
        />
    );
}
