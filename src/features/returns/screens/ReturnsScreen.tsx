import PlaceholderScreen from "../../../shared/components/PlaceholderScreen";

export default function ReturnsScreen() {
    return (
        <PlaceholderScreen
            title="Returns"
            description="Manage return and exchange requests from customers."
            icon="Undo2"
        />
    );
}
